version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aka-anka-app
    env_file:
      - .env.production
    ports:
      - "3000:3000"
    restart: unless-stopped
